# Day 8 hardening plan

Pricing V2 remains the source of truth. Production mock fallback is disabled. Booking will move from route labels to stable Route IDs, and pricingByVehicle remains a temporary compatibility adapter until existing consumers are migrated.